// space
const ABSOLUTE_WIDTH = 640;
const ABSOLUTE_HEIGHT = 384;
const GROUND_DEPTH = 30;
const BASE = ABSOLUTE_HEIGHT - GROUND_DEPTH;

// time
const FPS = 20;
const SEC = 30;
const FPC = FPS / SEC;

// force
const GRAVITY = 0.6;

/**
 * ABSOLUTE_WIDTH: Measure of the game window's width
 * ABSOLUTE_HEIGHT: Measure of the game window's height
 * GROUND_DEPTH: Distance between floor and game window's bottom
 * BASE: Position of the floor where objects stand
 * FPS: Frames per second
 * FPC: Frames per clock cycle
 * SEC: Number of clock cycles in one second
 * GRAVITY: Acceleration due to gravity
 */